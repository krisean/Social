import { test, expect } from "@playwright/test";

test.describe("Global How to Play modal", () => {
  test.skip("opens from the floating action and lists steps", async ({ page }) => {
    // Skip this test - how-to-play button not implemented yet
  });
});
